# Intake lab — end-to-end test report (2026-04-25)

Branch: `test/regional-editorial-contributor-intake`
Preview: <https://test-regional-editorial-cont.esrf-clean.pages.dev/submit-validation.html>
Backend code under test: `functions/api/intake.js` (commit `b20bad6`)
Apps Script reference: `docs/apps-script-intake-webhook.gs`

This report records the manual + automated dry-run evidence that the
lab automation (workflow + LAB_* tabs + PII-free notification) behaves
as documented in [`docs/intake-lab-automation.md`](./intake-lab-automation.md).
It also captures the operational checklist that must be satisfied
before the redactie activates the real Apps Script webhook.

> **Lab only.** No production publication, no row written to
> `Directory_Master`, no editorial published. All test data is fictitious.

---

## 1. Tested scenario

End-to-end browser dry-run via the Cloudflare Pages preview build.

| Field | Value |
|---|---|
| Mode (`intake_mode`) | `both` (organisation + editorial) |
| Organisation name | `ESRF Lab Test Organisation` (dummy) |
| Country | `NL` |
| Region | `Zuid-Holland` |
| Place | regional dropdown selection (known place — no candidate row expected) |
| Editorial topic / summary / regional angle | dummy lorem-style placeholders |
| Consents (gdpr, editorial may contact) | both ticked |
| Submission id | `sub_moelllvt_i21b` |
| Request id | `req_moelllvt_19wf` |
| Test class | dummy / no publication / no real PII |

The submission was generated by clicking the
**"Backend test — geen productieopslag tenzij validatiesecrets actief
zijn"** button in the preview panel.

---

## 2. Expected vs. observed

### Expected (per `docs/intake-lab-automation.md` §3)

- `workflow.status: "dry_run"` because no sheet webhook URL is set.
- `workflow.steps` contains five entries in order: `received`,
  `validated`, `stored_or_dry_run`, `notification_prepared_or_sent`,
  `next_required_action`.
- `sheet_dry_run: true`, with `sheet_webhook_payload_preview` showing
  `target_prefix: "LAB_"` and `Directory_Master` listed as forbidden.
- LAB row preview includes one row for `LAB_Intake_Submissions`, one
  for `LAB_Editorial_Intake` (because `mode=both`), plus a backend log
  row and a workflow event row.
- `notification_status: "dry_run_not_configured"` (no
  `INTAKE_NOTIFY_WEBHOOK` set), with a `notification_message` carrying
  **only** the documented minimal fields.
- No `Directory_Master` reference anywhere in the success payload
  except as a forbidden target.

### Observed

All expectations met. Specifically:

- Workflow status: `dry_run`.
- All five workflow steps present, in the documented order.
- `next_required_action` text: *"Configure `SHEETS_WEBHOOK_URL` or
  `INTAKE_SHEET_WEBHOOK_URL` (and optionally
  `SHEETS_WEBHOOK_SECRET`) on Cloudflare Pages → Settings →
  Environment variables (Preview) so the webhook can write to the
  LAB_* tabs."*
- Sheet preview lists the spreadsheet id
  `1jGDFjTq5atrFSe3avjj4AflUo1SLPKAmkT_MIpH6z1g`,
  `target_prefix: "LAB_"`, and `forbidden_targets: ["Directory_Master"]`.
- Notification: `dry_run_not_configured`, message preview contains
  schema_version / submission_id / request_id / environment / mode /
  type / org_name / country / region / workflow_status /
  next_required_action / related_sheet / note. **No** contact email,
  contact name, contact phone, summary, regional_angle, or lesson.
- LAB row preview entries present for `LAB_Intake_Submissions`,
  `LAB_Editorial_Intake`, `LAB_Backend_Log`, `LAB_Workflow_Events`.
  No `LAB_Place_Candidates` row (place was a known one — matches
  documented behaviour).
- `Directory_Master` appears only inside `forbidden_targets`.

### UI issue found and fixed

The combined path showed two fieldsets both labelled "**3.**":
`3. Organisatie-gegevens` and `3. Editorial bijdrage`. This made the
flow ambiguous when `mode=both`.

Fix applied in this report's commit:
- `submit-validation.html`: relabel to `3A. Organisatie-gegevens` and
  `3B. Editorial bijdrage`, and added one explanatory sentence in
  each fieldset's intro so the order is unambiguous regardless of
  which mode the user picks.
- No layout, copy elsewhere, or backend behaviour was changed.
- The live-like ESRF look (mast, hero, foundation block, footer) and
  the subtle staging markers (TEST/VALIDATIE banner, "Nieuw"-pillen,
  noindex/nofollow) are preserved.

---

## 3. Automated test coverage (Node-only, no preview required)

```
node functions/api/intake.test.mjs
node functions/_middleware.test.mjs
```

Both suites pass on this commit. Notable cases relevant to this
report:

- `notification message excludes PII and editorial body` — proves the
  notification contract.
- `LAB spreadsheet config forbids Directory_Master writes` — proves
  the safety net.
- `combined route emits both intake row and editorial row` — covers
  the `mode=both` row fan-out.
- `unknown place triggers a LAB_Place_Candidates row` /
  `known place does NOT trigger a place candidate row` — covers
  the place-candidate branch.
- `nextRequiredAction returns dry-run copy when sheet not configured`
  — matches the human-readable string observed in the browser test.

---

## 4. Notification contract — process-step expectations

For each successful `POST /api/intake` the redactie should see, in
order, these workflow steps in the response (and — once
`INTAKE_NOTIFY_WEBHOOK` is active — a single ping carrying only the
minimal fields):

| Step | What the redactie should see | Notification triggers? |
|---|---|---|
| `received` | "Submission received and assigned `submission_id` / `request_id`." | no — internal only |
| `validated` | "Required fields, consents and country/region passed validation." | no |
| `stored_or_dry_run` | `stored` (sheet write succeeded, row id present) or `dry_run` (no webhook URL configured) or `error`. | no |
| `notification_prepared_or_sent` | `sent` (webhook accepted), `dry_run_not_configured` (no webhook URL), or `error`. | yes — when `sent` |
| `next_required_action` | Human-readable next step (e.g. "Configure `SHEETS_WEBHOOK_URL`…", or "Pick up the row in `LAB_Intake_Submissions`."). | no |

**Notification body — what it MUST contain**

`schema_version`, `submission_id`, `request_id`, `environment`,
`mode`, `type` (`org` / `editorial` / `org+editorial`), `org_name`,
`country`, `region`, `workflow_status`, `next_required_action`,
`related_sheet`, `related_row` (when known), `issue_url` (when
GitHub evidence is configured), `note`.

**Notification body — what it MUST NOT contain**

No `contact_email`, no `contact_name`, no `contact_phone`,
no `editorial.summary`, no `editorial.regional_angle`,
no `editorial.lesson`, no other free-form editorial body. The
notification is a pointer back to the spreadsheet — it never
duplicates editorial text or PII. Enforced by the test
`notification message excludes PII and editorial body` in
`functions/api/intake.test.mjs`.

---

## 5. Known limitation — Cloudflare preview secrets not configured

The preview environment has none of the optional secrets set, so the
backend stays in full dry-run:

| Variable | State | Effect |
|---|---|---|
| `INTAKE_SHEET_WEBHOOK_URL` (or `SHEETS_WEBHOOK_URL`) | unset | `sheet_dry_run: true`, no row written |
| `SHEETS_WEBHOOK_SECRET` | unset | n/a while URL is unset |
| `INTAKE_NOTIFY_WEBHOOK` | unset | `notification_status: dry_run_not_configured` |
| `TURNSTILE_SECRET_KEY` | unset | Turnstile verification skipped (warning emitted) |
| `GITHUB_TOKEN` + `INTAKE_REPO` | unset | `issue_dry_run: true`, no GitHub issue created |

This is the **expected** lab posture: nothing is written, the
response shows the redactie exactly what *would* be sent. The browser
test confirmed this matches the documentation.

---

## 6. Operational readiness checklist — next manual activation steps

The lab is ready for webhook activation. The remaining steps are
operational, not code:

1. **Deploy the Apps Script** in `docs/apps-script-intake-webhook.gs`
   against the existing Drive spreadsheet
   (`1jGDFjTq5atrFSe3avjj4AflUo1SLPKAmkT_MIpH6z1g`). Confirm the five
   `LAB_*` tabs already exist (they do — verified 2026-04-25). Publish
   as a **Web App** ("Deploy → New deployment → Web app", execute as
   the script owner, access "Anyone with the link").
2. **Generate a shared secret** (e.g. `openssl rand -hex 32`). The
   Apps Script must compare the inbound header `x-esrf-intake-secret`
   against this value and reject mismatches.
3. **Set Cloudflare Pages → Settings → Environment variables (Preview
   only, `test/regional-editorial-contributor-intake`):**
   - `INTAKE_SHEET_WEBHOOK_URL` = the deployed Web App URL
   - `SHEETS_WEBHOOK_SECRET` = the shared secret from step 2
   - (Optional) `INTAKE_NOTIFY_WEBHOOK` = a Slack-compatible webhook
     for the redactie channel
   - (Optional) `TURNSTILE_SECRET_KEY`
   - **Do not** set these on Production until the redactie signs off.
4. **Re-run the browser dry-run** of step 1 above. Expected change:
   `workflow.status: stored`, a real row id from the sheet appears in
   `related_row`, and the `LAB_Intake_Submissions` tab shows the new
   row within ~2 seconds.
5. **Verify Directory_Master is untouched** — manually inspect the
   tab; no automation row should appear.
6. **Sign-off:** the redactie reviews the resulting LAB rows + the
   notification copy, then files the production-overgang as described
   in `docs/intake-lab-automation.md` §5.

Until step 3 is done, the lab continues to behave exactly as
described in §1–§4 of this report.

---

## 7. Files changed in this report's commit

- `submit-validation.html` — fix duplicate step-3 numbering
  (`3A` / `3B`), preserve all other UI.
- `docs/intake-lab-test-report-2026-04-25.md` — this document (new).
- `docs/intake-lab-automation.md` — link to this report.
- `validation-lab.json` — bump module status to
  *Ready for webhook activation* and expose this report in
  `documentation`.

No backend logic changed. Tests still green.
